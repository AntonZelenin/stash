[workspace]
resolver = "2"
members = [
    "packages/api",
    "packages/ui",
    "packages/web",
    "packages/desktop",
    "packages/mobile",
]

[workspace.dependencies]
dioxus = { version = "0.7.1", features = ["router"] }

# workspace
api = { path = "packages/api" }
ui = { path = "packages/ui" }

