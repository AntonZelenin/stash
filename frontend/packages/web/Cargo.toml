[package]
name = "web"
version = "0.1.0"
edition = "2024"

[dependencies]
dioxus = { workspace = true }
ui = { workspace = true }
api = { workspace = true }
serde_json = "1"
web-sys = { version = "0.3", features = ["Storage", "Window"] }

[features]
default = ["web"]
web = ["dioxus/web"]

